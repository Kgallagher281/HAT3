<p id="notice"><%= notice %></p>

<br><br>
<% @events.each do |event| %>

<!--   <div class="wrapper">
    <div class="main">

      <section class="page1"> -->
        
        <div class="page_container">
         <%= image_tag event.photo.url(:large), class: "img" %>
          <h1><%= event.name %></h1>
<br><br>
       
            <a href="#"><%= event.details %><br>
<br><br>
              <small><%= event.date %> <br>
<br><br>        
                    <%= event.address %>
              </small>
            </a>
            <%= link_to 'Show', event %>
        </div>
     <!--  </section>
    </div>
  </div> -->
<% end %>
